
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # سيتم النشر تلقائياً عند التحديث في فرع main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # المجلد الذي يحتوي على الملفات النهائية بعد البناء
          branch: gh-pages # الفرع الذي سيتم النشر عليه
